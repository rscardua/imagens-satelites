# Rust / Cargo
/target/
**/target/
*.pdb

# Mantemos Cargo.lock versionado (workspace de aplicação/binários).

# Variáveis de ambiente e segredos
.env
.env.*
!.env.example

# Claude Code — notas pessoais não compartilhadas
CLAUDE.local.md

# Editores / IDEs
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Artefatos de teste manual / debug (server-rust)
apps/server-rust/*.png
apps/server-rust/*.bin
apps/server-rust/sid.txt
apps/server-rust/sp.json
apps/server-rust/wpm_item.json
